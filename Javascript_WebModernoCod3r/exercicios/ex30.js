// 30) Escreva um algoritmo que percorre um vetor de inteiros e defina o maior e menor valor dentro do vetor.
// Resposta: OK

const numeros = [8, 4, 0, 3, 1, 839, 3, 11, 435]

console.log(Math.min(...numeros), Math.max(...numeros))