# [back] package ts-node, sobe o server express
    npx ts-node --ignore-watch node-modules src/server.ts

# [back] package ts-node-dev, sobe o server
    npx ts-node-dev --ignore-watch node-modules  src/server.ts

# [front] ferramenta somente executada que cria um projeto React completo
    npx create-react-app web --template=typescript

# [database] Knex.js, biblioteca de integração com base de dados
    npm install knex
    npm install sqlite3

    # utilizando migrations
        npx knex migrate:latest --knexfile knexfile.ts migrate:latest

    # instalar extensão do VsCode SQLite para visualizar o database criado
        [Windows] + [Ctrl] + [P] : SQLite -> Open Database
        Ira abrir o SQLite explorer na barra lateral com o resultado da base
        
        Para executar uma query, botão direito na base, new query
        [Windows] + [Ctrl] + [P] : SQLite -> Run Query
    
    # popular base de dados com valores default, utilizamos o recurso seed do Knex
        knex --knexfile knexfile.ts seed:run

# [back] tratar diretorios no node, usar biblioteca nativa
    import path from 'path'
    path.resolve()

# [back] padrão da comunidade para rotas em controllers
    index: lista de itens
    show: item unico
    create ou store: criar item
    update: atualizar item
    delete ou destory: excluir item

# [back] incluindo CORS
    npm i cors
    npm i @types/cors -D

# [front] configuração de emmet javascript VsCode
    [Windows] + [Ctrl] + [P] : Open Settings (JSON), incluir as linha abaixo
    "emmet.syntaxProfiles": { "javascript": "jsx" },
    "emmet.includeLanguages": { "javascript": "javascriptreact" },

# [front] JSX, permite colocar HTML dentro do javascript
    arquivo com extensão .tsx ou .jsx

# [front] pacote de ícones do react
    npm i react-icons

# [front] pacote para roteamento do react
    npm i react-router-dom
    npm i @types/react-router-dom -D

# [front] pacote para integração com mapa
    npm i leaflet 
    npm i react-leaflet
    npm i @types/react-leaflet -D

# [front] pacote para fazer requisições no backend
    npm i axios

# [mobile] pacote expo-cli para rodar app no celular
    npm i -g expo-cli
    criar projeto no mobile
    expo init mobile

# [mobile] todos os elementos são display flex, não tem cascata de estilo (css)

# [mobile] pacote que permite utilizar as fontes do google
    expo install expo-font @expo-google-fonts/[nomeFonte]